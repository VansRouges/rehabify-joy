"""Schema changes live in alembic/versions/. Do not add ad-hoc SQL here.

From backend/:

    alembic revision -m "add_column_foo"

Edit the new file under alembic/versions/, then deploy. Startup runs
`alembic upgrade head`.
"""
